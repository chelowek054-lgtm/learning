# Оркестрация docker-compose (суперпроект). Скопировать в .env (в git не коммитится).
# Параметры сервисов стенда. Секреты реальных окружений — не сюда.

# Postgres
POSTGRES_USER=praxis
POSTGRES_PASSWORD=praxis
POSTGRES_DB=praxis
POSTGRES_PORT=5432

# API (learningBack)
API_PORT=8000
APP_ENV=development
