# Praxis — RoadMap

> **Единая точка фиксации прогресса.** Этот файл отвечает на три вопроса: *где мы сейчас*, *что уже сделано (с доказательствами)* и *что дальше*. Обновляется при каждом закрытом рабочем потоке.

**Снимок на:** 2026-08-23 · **Текущая фаза:** Фаза 2 — Модель знаний · **Текущий поток:** KG3 (генерация оценок из теории узла)

Архитектура — [`docs/architecture/`](./docs/architecture/README.md) · исполняемые чеклисты фаз — [`docs/plans/`](./docs/plans/README.md) · видение и инварианты — [`docs/README.md`](./docs/README.md).

---

## 1. Дашборд

| Фаза | Статус | Прогресс | Что даёт продукту |
|---|---|---|---|
| **[Фаза 0 — Каркас](./docs/plans/phase-0-foundation.md)** | ✅ done | 34/34 задач · 6/6 потоков | Связный запускаемый скелет: суперпроект+сабмодули, FSD-клиент, доменно-независимое ядро, FastAPI+Postgres, оркестрация |
| **[Фаза 1 — MVP](./docs/plans/phase-1-mvp.md)** | ✅ done (код) | 33/33 задач · 8/8 потоков | Вертикальный срез: JWT, offline-first sync+jobs, AI-скоринг по рубрикам, Writing + Vocab SRS + ML-трек, онбординг и Home |
| **[Фаза 2 — Модель знаний](./docs/plans/phase-2-knowledge-model.md)** | 🟡 **в работе** | 9/21 задач · 2/5 потоков | Граф концепций как ядро-актив: канон + персонал (COW), плейсмент, генерация курсов |
| **Фаза 3 — Речь и рецепция** | ⚪ ожидает | не декомпозирована | Speaking (STT + рубрика), listening/reading-дриллы, генерация passages |
| **Фаза 4 — Продукт** | ⚪ ожидает | не декомпозирована | Курирование канона, мульти-девайс, монетизация, публикация (EAS) |

**Итого по декомпозированному объёму (Ф0–Ф2): 76 из 88 задач — 86 %.**

```
Ф0  ████████████████████  100%   каркас
Ф1  ████████████████████  100%   MVP (вертикальный срез)
Ф2  █████████░░░░░░░░░░░   43%   модель знаний   ← здесь
Ф3  ░░░░░░░░░░░░░░░░░░░░    0%   речь и рецепция
Ф4  ░░░░░░░░░░░░░░░░░░░░    0%   продукт
```

### Изменение приоритетов (зафиксировано)

Роадмап пере-приоритизирован относительно исходного ([03 §5](./docs/architecture/03-functional.md#5-roadmap)):

- **Модель знаний вытянута вперёд** — стала Фазой 2. Причина: без графа активности изолированы, система не знает, *как знания связаны*, и не может ни измерить уровень, ни построить путь до цели.
- **Прежняя «Адаптивность» поглощена** Фазой 2: курсы и плейсмент — это и есть адаптивность; PDF→граф — заземление узлов (`source='material'`).
- **Речь и рецепция сдвинулись** с Фазы 2 на Фазу 3.

---

## 2. Как вести этот файл

### Легенда статусов

| | Значение |
|---|---|
| ✅ `done` | завершено **и** удовлетворяет Definition of Done (есть проверка, результат зафиксирован честно) |
| 🟡 `wip` | в работе |
| ⚪ `todo` | не начато |
| 🔴 `blocked` | заблокировано (указать чем) |

### Идентификаторы

`P<фаза>-<поток>-<номер>` — например `P0-WS1-03`, `P2-KG2-01`. Потоки внутри фазы независимы и могут вестись параллельно.

### Правила обновления

1. **Прогресс фиксируется здесь и в плане фазы.** Детальные чеклисты живут в [`docs/plans/phase-*.md`](./docs/plans/README.md); этот файл — сводка и журнал.
2. **`done` только с доказательством.** Указывать способ проверки: *live-прогон* (реальный запуск), *typecheck+bundle* (собралось, но рантайм не прогонялся), *тест*. Не выдавать одно за другое.
3. **Изменение архитектуры — сначала в `docs/architecture/`,** затем в план фазы, затем сюда.
4. **Закрыл поток → добавь строку в [Журнал](#7-журнал-прогресса)** с датой, объёмом и способом проверки.
5. **Разрешённый открытый вопрос** переносится в архитектурный документ, а в [реестре](#5-реестр-открытых-вопросов) помечается решением.

---

## 3. Проделанная работа

### 3.1 Фаза 0 — Каркас · ✅ done · 34/34

**Цель:** пустой, но связный и запускаемый скелет всей системы. Ни одной обучающей фичи — но есть сквозной контур: клиент стартует и показывает зарегистрированные типы Activity, backend отвечает, БД мигрируется.

| Поток | Статус | Итог | Проверка |
|---|---|---|---|
| `WS1` Каркас репозитория + фронт-тулинг | ✅ 6/6 | Суперпроект с сабмодулями, FSD-скелет `src/{app,pages,widgets,features,entities,shared}`, алиас `@/*`, тулинг `ts-fsrs`/`vitest`/`prettier` | `npm install`; typecheck/test/format зелёные |
| `WS4` Ядро `shared/engine` | ✅ 7/7 | Типы `Activity`/`Connectivity`/`Grade`/`Response`; контракты `ModuleManifest`/`ActivityTypeDef`/`LocalGrader`; `ModuleRegistry` (табличный lookup, без `if module === …`); порты `LocalStore`/`SyncClient`/`JobQueue`; обёртка FSRS | tsc 0 ошибок; юнит-тесты; grep доменных строк по ядру — пусто |
| `WS5` Модули как FSD-слайсы + манифесты | ✅ 5/5 | `languages` (6 типов Activity) и `ml` (4 типа) как **чистые метаданные**; плейсхолдер-рендерер; composition root `initModuleRegistry()`; backend-заглушки модулей | tsc 0; тесты метаданных; коллизий типов нет |
| `WS6` SQLite LocalStore + стартовый экран | ✅ 5/5 | `SqliteLocalStore` на `expo-sqlite` + web-замена (in-memory); виджет `activity-dispatcher`; экран `pages/home` с типами, статусом БД и демо-диспетчеризацией | `expo export --platform android` → **EXIT 0** (Hermes-бандл). ⚠️ рантайм на устройстве не прогонялся |
| `WS2` Backend-скелет (FastAPI) | ✅ 6/6 | `core/` + `modules/`, `GET /health`, OpenAPI, конфиг `pydantic-settings`, Protocol `AIGateway`, Dockerfile + корневой `docker-compose.yml` | **live:** `docker compose up` → postgres healthy + api; `/health` = `{status: ok}`; ruff чист |
| `WS3` Схема БД (Postgres + Alembic) | ✅ 5/5 | Модели и миграция `0001_init`: `user`, `activity`, `response`, `srs_card`, `job`, `material`, `rubric` + 4 индекса; seed-заглушка | **live:** `alembic upgrade head` создал 7 таблиц + 4 индекса; seed записал user + 2 рубрики через ORM |

**Ключевые технические решения фазы:**

- **uv** вместо poetry для Python (закрыт открытый вопрос).
- **raw `expo-sqlite`** вместо drizzle-orm — drizzle-миграции требуют codegen и Metro-конфиг под `.sql`, непроверяемо без устройства; порт `LocalStore` абстрагирует БД, замена возможна позже без изменения ядра.
- **Платформенная замена** `sqlite-local-store.web.ts` (in-memory) — `expo-sqlite` на web тянет `wa-sqlite.wasm` (COEP/OPFS), избыточно для mobile-first.
- **Оркестрация в корне суперпроекта**, `Dockerfile` — в сабмодуле сервиса. Новый сервис = блок в корневом compose + свой Dockerfile в его репозитории.
- **Понижение Expo SDK 57 → 54** (RN 0.81, React 19.1, TS 5.9) — ради совместимости с публичным Expo Go на устройстве.

---

### 3.2 Фаза 1 — MVP · ✅ done (код) · 33/33

**Цель:** вертикальный срез, доказывающий главную гипотезу — *AI-скоринг продукции по рубрикам эффективнее изолированных карточек* — и универсальность движка (один Activity-движок тянет и язык, и ML). Всё offline-first.

**Сквозной сценарий, который собран:**

```
регистрация (свой JWT) → онбординг (цели)
 → Writing: эссе ОФЛАЙН → мгновенный черновой сигнал (локальный грейдер)
   → появилась сеть → sync → AI-скоринг по рубрике IELTS (4 критерия + ошибки + образец)
   → ошибки автоматически → карточки vocab_srs
 → SRS: повторение, интервалы по FSRS (офлайн)
 → ML: material_read → concept_recall → AI-проверка → слабые концепты в SRS
```

#### Фундамент

| Поток | Статус | Итог | Проверка |
|---|---|---|---|
| `WS1` Аутентификация (собственный JWT) | ✅ 5/5 | Миграция `0002` (`password_hash`, argon2 через `pwdlib`); `/auth/register`, `/auth/login`, `/auth/me`, `PUT /auth/me/profile`; зависимость `get_current_user`; защита `sync`/`jobs`/`content`. Клиент: `expo-secure-store`, http-клиент с `Bearer`, `entities/session`, экраны `pages/auth`, route-guard | backend live; клиент typecheck+bundle |
| `WS2` Sync + Job Queue (offline-first мост) | ✅ 4/4 | `POST /sync/push` + `GET /sync/pull` (per-user, LWW, идемпотентность по `id`); персист jobs и переходы `pending→running→done/failed`. Клиент: реализация портов `SyncClient`/`JobQueue` поверх SQLite, детекция сети `expo-network`, триггеры sync, применение результатов pull | backend live; клиент typecheck+bundle |
| `WS3` AI-gateway (Claude) | ✅ 4/4 | `AIGateway.grade/generate` через Claude со **structured output** (tool-use по `rubric.schema`), выбор модели per-rubric, реестр рубрик из БД, валидация результата в строгий `Grade`, логирование токенов, кэш детерминированной генерации по хэшу входа; `MockAIGateway` за тем же интерфейсом | Mock live; реальный Claude — при `CLAUDE_API_KEY` |
| `WS4` Рубрики и генераторы контента | ✅ 4/4 | Рубрика `ielts_writing_task2` v1 (Task Response / Coherence & Cohesion / Lexical Resource / Grammar → band); рубрика `concept_check` (correctness / completeness / explanation); генераторы: стартовая колода AWL, маппинг `grade.errors → srs_card`, `concept_recall` из `material`; seed демо-контента | live seed + скоринг |

#### Вертикальные фичи и опыт

| Поток | Статус | Итог | Проверка |
|---|---|---|---|
| `WS5` Writing `ielts_writing_task2` | ✅ 5/5 | `features/ielts-writing`: редактор эссе со счётчиком слов; **офлайн-fallback `LocalGrader`** (объём, структура абзацев, покрытие AWL); submit → `appendResponse` + `enqueueJob(grade_writing)`; экран разбора (4 критерия band + ошибки + образец); error-log → `vocab_srs` | typecheck+bundle |
| `WS6` Vocab SRS `vocab_srs` | ✅ 4/4 | `features/vocab-review`: карточка front/back + again/hard/good/easy; `Scheduler.review` → обновление `fsrs_state` и `due_at` в `LocalStore`; очередь «на сегодня» из `listDueSrsCards`; колода питается из error-log и AWL | typecheck+bundle |
| `WS7` ML-трек `material_read` + `concept_recall` | ✅ 3/3 | `features/material-read` (чтение закэшированного `material`); `features/concept-recall` (вопрос → открытый ответ → `enqueueJob(grade_concept)`); разбор AI-проверки → слабые концепты в `concept_srs` | typecheck+bundle |
| `WS8` Онбординг + Home + навигация | ✅ 4/4 | Онбординг целей (target band, ML-темы) → `user.profile`; Home = «на сегодня» через `ActivityDispatcher`; табы Home/Review/Learn/Profile + route-guard; простой адаптивный план (слабейший критерий из `response`-лога → предложить фокус; правило, не ML) | typecheck+bundle |

> ⚠️ **Честная оговорка по Фазе 1.** Backend прогнан вживую (docker compose, реальные запросы, миграции, seed). Клиент проверен `tsc --noEmit` (0 ошибок), юнит-тестами (12/12) и сборкой бандла (`expo export`, web + android). **Рантайм-прогон на устройстве или эмуляторе не выполнялся** — среда разработки его не даёт. Сквозной пользовательский прогон из чеклиста фазы остаётся за ручной проверкой.

---

### 3.3 Фаза 2 — Модель знаний · 🟡 в работе · 9/21

**Цель:** построить слой модели знаний (граф концепций как ядро-актив) и доказать на ML цепочку: *канонический граф → наследование новым пользователем (COW) → адаптивный плейсмент → генерация курса до целевого уровня*, где узел хранит формализованную теорию, из которой генерятся тест и практика.

Архитектура слоя — [05 — Модель знаний](./docs/architecture/05-knowledge-model.md). Слой лежит **над** движком Activity; ядро остаётся доменно-независимым — граф это данные модуля, а не логика `shared/engine`.

**Зафиксированные решения фазы:**

- **Первая тема** — ML/программирование (граф естественно иерархичен).
- **Источник графа** — LLM генерирует → пользователь правит (Approve / Edit / Reject).
- **Канон ↔ персонал** — **copy-on-write**: новый пользователь читает канон сквозняком, правка или рост создают персональный узел поверх; улучшения канона видны всем, у кого узел не перекрыт.
- **Тир узла** — `core` (фундаментальное **обязательное** ядро) vs `derived` (вытекающие ветви поверх ядра).
- **Курс = симуляция развития** (укоренение → дифференциация → ветвление → спираль), а не топосорт.

#### Сделано

| Поток | Статус | Итог | Проверка |
|---|---|---|---|
| `KG1` Данные графа (canonical + personal, COW) + CRUD | ✅ 4/4 | Миграция `0003_knowledge_model`: `concept` (+`tier`, `centrality`), `concept_edge`, `user_concept`, `user_edge`, `assessment`, `course`. COW-чтение `core/knowledge.py` (`effective_graph`: override → иначе канон). CRUD-API `/graph`: эффективный граф, персональный CRUD, канон-курирование с версионированием узла, `canon/build`. `AIGateway` расширен `build_graph`/`expand_node`; Mock даёт детерминированный ML-черновик с пометкой ядра | **live:** миграция применена; build канон → новый юзер наследует через COW (все `inherited`, без копий) → override даёт `edited` + персональный контент → свой узел + ребро + mastery; ruff чист |
| `KG2` Построение и рост графа + редактор | ✅ 5/5 | `ClaudeAIGateway.build_graph` через tool-use (узлы + `prereq`/`specializes`, `tier`, `confidence`, `status='draft'`); **core-детекция** — `core/graph_service.recompute_centrality` (descendant-count по prereq/specializes) + гибрид `suggestedCore` (метрика ≥ 0.5 ИЛИ пометка LLM); эндпоинты `recompute-centrality` и `approve` (governance); `expand_node` + `/graph/expand` → персональные own-узлы и рёбра (COW, `origin='grown_llm'`). Клиент: `entities/concept`, `features/graph-editor` (список по тирам + фокус-узел с соседями, **без canvas** — UX под телефон), редактор content (override/patch), «В ядро», «Углубиться», экран `pages/graph`, вход из Home | **backend live:** build → recompute-centrality (Линал 1.0 → Трансформеры 0.0; Backprop core-suggested по пометке, Softmax нет) → approve → expand (рост ветки COW). Клиент: typecheck 0, тесты 12/12, web+android bundle |

Итог по API: контейнер отдаёт **14 эндпоинтов `/graph`** (чтение эффективного графа, персональный CRUD, канон-курирование, build/expand/recompute/approve).

#### Осталось в фазе

| Поток | Статус | Объём | Definition of Done |
|---|---|---|---|
| `KG3` Узел-контент → генерация оценок | ⚪ todo · 3 задачи | Структура `content` узла (summary / sections / examples / counter-examples / references); AI-роль `generate_assessment(content, bloom, kind)` → тест-айтемы (recall/understand) и практика (apply/create), **заземлённые на content**, с тегами Блума; кэш `assessment` по `(concept_id, version, bloom, kind)` | Из теории узла детерминированно генерятся валидные тест и практика; кэш работает; смена версии узла инвалидирует кэш |
| `KG4` Адаптивный плейсмент | ⚪ todo · 4 задачи | Модель освоенности: приор из предпосылок + байесовское обновление по ответам (**не IRT**), хранение в `user_concept.mastery`; выбор зонда на границе знаний (макс. неопределённость) + ступень Блума; оценка ответа через `estimate_mastery`, стоп по уверенности/бюджету; клиент — онбординг-плейсмент, где **шаг 1 — целевая ступень** | Пользователь задаёт цель → проходит адаптивный тест → получает карту освоенности узлов и «границу» |
| `KG5` Генерация курса + прогресс | ⚪ todo · 5 задач | **Укоренение**: непокрытое `tier=core` вставляется первым, независимо от цели. **Дифференциация**: от фронтира по `prereq`/`specializes` (общее→частное) в ЗБР; **ветвление** `derived`-веток под интересы; сохранение `course.path`. **Спираль и починка**: пере-подъём ядровых узлов выше по Блуму, `misconception`-рёбра → контраст-активности; под узел — цепочка `concept_study → concept_recall → apply → srs` + interleaving. Клиент: экран курса и прогресса; интеграция FSRS, ретеста и error-log | От карты mastery строится курс до цели; активности идут по графу существующим `ActivityDispatcher`; FSRS держит удержание; ретест пересобирает путь |

**Проверки инвариантов в конце фазы** (ещё не прогонялись):

- [ ] `shared/engine` без графа и доменных строк — граф только в модуле и на backend.
- [ ] Оценки заземлены на `content` узла (контент присутствует в промпте генерации).
- [ ] Персональные правки и рост не мутируют канон (COW); улучшение канона видно там, где нет оверрайда.
- [ ] `assessment` хранит `concept_version`; смена версии инвалидирует кэш.

---

### 3.4 Фаза 3 — Речь и рецепция · ⚪ ожидает

Не декомпозирована. Планируемый объём:

- `speaking_response` — устный ответ → STT → скоринг по рубрике `ielts_speaking` (Fluency, Lexical Resource, Grammar, Pronunciation).
- `listening_drill` / `reading_drill` — экзаменационный формат на время; детерминированная проверка (MCQ) офлайн локально, разбор «почему дистрактор неверен» — online-обогащение при сети.
- Генерация passages и аудио-материалов; кэширование аудио для офлайна.
- **Предусловие:** решить открытый архитектурный вопрос — Whisper на сервере vs on-device STT.

### 3.5 Фаза 4 — Продукт · ⚪ ожидает

Не декомпозирована. Планируемый объём:

- **Курирование канона:** промоция качественных персональных узлов в канонический граф.
- **Восстановление пароля — доставка кода.** Механика уже есть: `POST /auth/password-reset/request` кладёт 8-значный одноразовый код в `password_reset_code` с TTL из конфига, `POST /auth/password-reset/confirm` меняет пароль. Не хватает трёх вещей:
  - `P4-AUTH-01` доставка кода на **почту** (SMTP/провайдер) и опционально **SMS**; выбор провайдера и его лимиты;
  - `P4-AUTH-02` экран восстановления в клиенте (запрос кода → ввод кода → новый пароль), вход из `pages/auth`;
  - `P4-AUTH-03` ужесточение под публичный доступ: **хешировать код** (сейчас в открытом виде — это осознанно, чтобы читать из pgAdmin без доставки), rate-limit на запрос кода по email и IP, инвалидация активных сессий после смены пароля.
- Мульти-девайс (стратегия конфликтов sync за пределами LWW), аккаунты.
- Монетизация; публикация в сторах через **EAS Build** + OTA-обновления через EAS Update.
- Managed Postgres (Supabase / Neon), продовый хостинг backend (Fly.io / Railway / VPS).

### 3.6 Сквозные треки (вне фаз)

| Трек | Статус | Что нужно |
|---|---|---|
| `docs/business/` — бизнес-модель, монетизация, аудитория, метрики продукта | ⚪ планируется | Секция объявлена в [`docs/README.md`](./docs/README.md), файлов нет |
| `docs/deploy/` — окружения, CI/CD, инфраструктура, релизы | ⚪ планируется | Секция объявлена, файлов нет |
| Политика приватности данных (что логируем из эссе и ответов, TTL) | 🔴 нужна до публичного релиза | Тексты пользователя уходят в Claude для скоринга — политика хранения не зафиксирована |
| Линтер границ FSD (`steiger` / `eslint-plugin-boundaries`) | ⚪ todo | Открытый вопрос с Фазы 0; сейчас границы держатся на grep и ревью |
| CI (typecheck / test / ruff на push) | ⚪ todo | Проверки прогоняются вручную |
| Рантайм-прогон клиента на устройстве | 🔴 открыт с Фазы 0 | Ни один клиентский поток не подтверждён рантаймом — только typecheck и bundle |

---

## 4. Что дальше

**Ближайший шаг — `KG3`:** структура `content` узла → `generate_assessment(content, bloom, kind)` → кэш `assessment` с привязкой к `concept_version`.

Почему именно он: `KG4` (плейсмент) не может подбирать зонды, пока их нечем генерировать, а `KG5` (курс) не может строить цепочки активностей без оценок. `KG3` — узкое место фазы.

Порядок остатка фазы: **KG3 → KG4 → KG5**, затем прогон инвариантов и сквозной сценарий фазы.

Параллельно, не блокируя фазу: рантайм-прогон клиента на устройстве (см. риски), линтер границ FSD, CI, политика приватности.

---

## 5. Реестр открытых вопросов

| # | Вопрос | Где влияет | Статус |
|---|---|---|---|
| 1 | **Core-детекция:** centrality графа / частота-как-предпосылки / вручную | KG1, KG2 | 🟡 частично решён — гибрид (метрика ≥ 0.5 ИЛИ пометка LLM) реализован в KG2; калибровка порога открыта |
| 2 | **Гранулярность узла** — что считать одним концептом | Фаза 2 целиком | ⚪ открыт (уточнять на реальном ML-графе) |
| 3 | **Модель освоенности** — скаляр на узел vs per-(узел, ступень Блума) | KG4 | ⚪ открыт, решать в KG4 |
| 4 | **Калибровка зондов** — эвристика сложности до перехода на IRT | KG4 | ⚪ открыт |
| 5 | **UX редактора графа на телефоне** — список vs canvas | KG2 | ✅ решён — список по тирам + фокус-узел, без canvas |
| 6 | **Двухуровневый граф для языкового домена** (язык менее иерархичен) | после Фазы 2 | ⚪ открыт |
| 7 | **Стоимость LLM** — лимиты, агрессивный кэш, выбор дешёвой модели | сквозной | 🟡 базово закрыт (кэш по хэшу входа, модель per-rubric); тюнинг по факту расходов |
| 8 | **Приватность** — политика хранения текстов на backend, TTL | до релиза | 🔴 открыт, блокирует публичный релиз |
| 9 | **Whisper на сервере vs on-device STT** | Фаза 3 | ⚪ открыт |
| 10 | **Конфликты sync при мульти-девайсе** (сейчас: один активный девайс, LWW) | Фаза 4 | ⚪ открыт |
| 11 | **Managed Postgres для прода** (Supabase / Neon) | Фаза 4 | ⚪ открыт — в dev локальный Docker-Postgres |
| 12 | **Границы FSD-импортов** — подключить линтер | сквозной | ⚪ открыт с Фазы 0 |

**Закрытые:** Auth → собственный JWT в FastAPI · менеджер Python → uv · ORM клиента → raw `expo-sqlite` за портом `LocalStore` · Expo SDK → 54 (совместимость с Expo Go) · источник Academic Word List → решён в WS4 · срок и секрет JWT → решены в WS1 · формат офлайн-fallback для Writing → решён в WS5.

---

## 6. Риски

| Риск | Влияние | Смягчение |
|---|---|---|
| **Клиент ни разу не прогнан в рантайме** — typecheck и bundle не ловят ошибки рендера, навигации, SQLite на устройстве | Высокое: накопленный объём Фаз 1–2 может разом развалиться при первом запуске | Прогнать на устройстве или эмуляторе **до старта KG5**, не откладывая до конца фазы |
| **Стоимость LLM** на генерацию графа и оценок | Среднее | Кэш `assessment` по версии узла (KG3), Mock для разработки, дешёвая модель где можно |
| **Граф «поплывёт»** без governance — LLM-шум попадёт в канон | Высокое для ядра-актива | LLM-узлы — `draft` с `confidence`; Approve / Edit / Reject; версионирование; `tier=core` подтверждает куратор |
| **Приватность** — тексты эссе уходят в Claude, политика не зафиксирована | Блокирует публичный релиз | Документ политики в `docs/deploy/` до Фазы 4 |

**Снятые риски:** ~~работа по KG1–KG2 не закоммичена~~ — закрыт 2026-08-23 (`step 4`: доки слоя + код в обоих сабмодулях, указатели сабмодулей в суперпроекте).

---

## 7. Журнал прогресса

Новые записи — **сверху**. Формат: дата · веха · объём · способ проверки.

| Дата | Веха | Объём | Проверка |
|---|---|---|---|
| 2026-08-23 | **Восстановление пароля (механика) + починка входа** | Backend: миграция `0004_password_reset`, 8-значный код с TTL из конфига, `request`/`confirm`-эндпоинты. Клиент: `NetworkError` отделён от `ApiError` — экран входа больше не выдаёт сетевой сбой за неверный пароль; `Authorization` не шлётся на login/register | live: код в БД (TTL 15 мин), неверный код → 400 + attempts, верный → 204, старый пароль 401 / новый 200, повтор кода → 400, несуществующий email → 202; ruff чист, typecheck 0 |
| 2026-08-23 | **Ф2 · KG1–KG2 закоммичены и слиты в `main`** — вместе с документацией слоя модели знаний | `step 4`: `05-knowledge-model.md` + `phase-2-knowledge-model.md` + 3 диаграммы; backend +897 строк (граф, миграция `0003`), клиент — граф-редактор и фичи Ф1; указатели обоих сабмодулей подняты | закоммичено, запушено, риск потери снят |
| 2026-08-23 | **RoadMap заведён** — единая точка фиксации прогресса | `ROADMAP.md` в корне + ссылка из `README.md`; PR #1 смержен в `main` | — |
| ~2026-07-05 | **Ф2 · KG2** — построение и рост графа + редактор на клиенте | 5 задач: `build_graph` (tool-use), core-детекция + centrality, `approve`, `expand_node`, `features/graph-editor` + `pages/graph` | backend live (build → recompute → approve → expand); клиент typecheck 0 / тесты 12/12 / web+android bundle |
| ~2026-07-05 | **Ф2 · KG1** — данные графа (canonical + personal, COW) + CRUD | 4 задачи: миграция `0003`, `effective_graph`, 14 эндпоинтов `/graph`, `AIGateway.build_graph`/`expand_node` | live: миграция применена; COW-цепочка inherited → edited → own проверена; ruff чист |
| ~2026-07-05 | **Ф2 · Архитектура** — слой модели знаний спроектирован | [`05-knowledge-model.md`](./docs/architecture/05-knowledge-model.md) + [`phase-2-knowledge-model.md`](./docs/plans/phase-2-knowledge-model.md) + 3 диаграммы; роадмап пере-приоритизирован | — |
| 2026-07-04 | **Ф1 · WS5–WS8** — вертикальные фичи и опыт | 16 задач: Writing + офлайн-грейдер, Vocab SRS, ML-трек, онбординг + Home + навигация + простой адаптивный план | клиент typecheck+bundle |
| 2026-07-04 | **Ф1 · WS1–WS4** — фундамент MVP | 17 задач: JWT-auth, sync + job-queue, AI-gateway (Claude + Mock), рубрики + генераторы + seed | backend live; клиент typecheck+bundle |
| 2026-07-04 | **Ф0 · WS2–WS3** — backend поднят | 11 задач: FastAPI-скелет, `/health`, Alembic `0001_init` (7 таблиц + 4 индекса), seed, Docker + корневой compose | live: `docker compose up`, `alembic upgrade head`, seed через ORM |
| 2026-07-04 | **Ф0 · WS1, WS4–WS6** — клиент и ядро подняты | 23 задачи: FSD-скелет, `shared/engine` (типы, реестр, порты, FSRS), модули-манифесты, SQLite LocalStore, стартовый экран | typecheck 0; юнит-тесты; `expo export --platform android` EXIT 0 |

---

## 8. Как проверить состояние

Весь стенд (Postgres + миграции + API → http://localhost:8000/docs):

```bash
docker compose up --build
```

Клиент — типы и тесты:

```bash
cd learningFront && npm run typecheck && npm run test
```

Backend — линтер:

```bash
cd learningBack && uv run ruff check .
```

Клиент на телефоне/в браузере ходит по `EXPO_PUBLIC_API_BASE_URL` из `learningFront/.env` — там **захардкожен IP машины**, который меняется при смене сети (Wi-Fi ↔ Ethernet ↔ хот-спот). Если вход «не проходит», сперва сверить адрес:

```bash
ipconfig | grep -A2 "IPv4"
```

Инвариант доменной независимости ядра — вывод должен быть пустым:

```bash
grep -rniE "languages|ielts|toefl|concept_edge" learningFront/src/shared/engine
```

---

## 9. Метрики эффективности продукта

Читаются из единого `response`-лога (инвариант №4) — отдельная аналитическая инфраструктура не нужна.

- **Удержание (SRS):** доля карточек, вспоминаемых с первого раза, во времени.
- **Рост по рубрикам:** динамика band/score продукции по критериям.
- **Закрытие error-log:** как быстро ошибки перестают повторяться после попадания в SRS.
- **Регулярность:** длина серий ежедневных сессий как прокси вовлечённости.
- **(с Фазы 2) Объём знаний:** размер выращенного подграфа (узлы × глубина × ступень Блума); **уровень** = положение фронтира.

---

## 10. Ссылки

| | |
|---|---|
| Видение, инварианты, глоссарий | [`docs/README.md`](./docs/README.md) |
| Архитектура (01–05) | [`docs/architecture/README.md`](./docs/architecture/README.md) |
| Планы фаз (исполняемые чеклисты) | [`docs/plans/README.md`](./docs/plans/README.md) |
| Текущий план фазы | [`docs/plans/phase-2-knowledge-model.md`](./docs/plans/phase-2-knowledge-model.md) |
| Клиент (FSD) | [`learningFront/src/README.md`](./learningFront/src/README.md) |
