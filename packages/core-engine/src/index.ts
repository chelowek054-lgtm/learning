// @praxis/core-engine — публичный API ядра.
// Доменно-независимо: НИ ОДНОЙ строки, специфичной для languages/ml (инвариант №3).
// Наполняется в WS4. Пока — плейсхолдер, чтобы workspace связывался.

export const CORE_ENGINE_VERSION = '0.0.0';
