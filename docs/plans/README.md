# Планы разработки

Пошаговые планы реализации Praxis по фазам. Архитектурная основа — в [../README.md](../README.md), [../01-architecture.md](../architecture/01-architecture.md), [../02-logical.md](../architecture/02-logical.md), [../03-functional.md](../architecture/03-functional.md).

Планы — это **исполняемые чеклисты**, а не описание архитектуры. Если план расходится с архитектурными доками — правится план; изменение архитектуры сначала фиксируется в доках уровнем выше.

---

## Фазы

| План | Статус | Содержание |
|---|---|---|
| [Фаза 0 — Каркас](./phase-0-foundation.md) | ✅ done | Суперпроект+сабмодули, FSD-клиент, ядро, FastAPI+Postgres, оркестрация — всё поднято и проверено |
| [Фаза 1 — MVP](./phase-1-mvp.md) | ✅ done (код) | Все WS1–WS8: JWT, sync+job-queue, AI-gateway, рубрики, Writing+Vocab SRS+ML-трек, онбординг+Home. Backend live; клиент typecheck+bundle (рантайм на устройстве — за пользователем) |
| [Фаза 2 — Модель знаний](./phase-2-knowledge-model.md) | 🟡 в работе (KG1–KG2 ✅) | Граф концепций (канон+персонал, COW). **KG1** (схема+COW+CRUD) и **KG2** (build_graph+core-детекция, expand, редактор-клиент) готовы. Дальше KG3–KG5: оценки из теории, плейсмент, генерация курса. Тема: ML |
| Фаза 3 — Речь и рецепция | ⚪ ожидает | Speaking (STT), listening/reading-дриллы, генерация passages |
| Фаза 4 — Продукт | ⚪ ожидает | Курирование канона (промоция узлов), мульти-девайс, аккаунты, монетизация, публикация |

> Роадмап пере-приоритизирован: **модель знаний** вытянута вперёд (Фаза 2). Прежняя «Адаптивность» (адаптивный план, PDF-импорт) поглощена ею — курсы/плейсмент = адаптивность, а PDF→граф = заземление узлов (KG2, `source='material'`). Речь/рецепция сдвинулись на Фазу 3. Исходный роадмап — [03 §5](../architecture/03-functional.md#5-roadmap).

---

## Соглашения планов

### Идентификаторы задач
Формат `P<фаза>-<поток>-<номер>`, например `P0-WS1-03`.
Потоки (workstreams) — независимые дорожки внутри фазы, могут вестись параллельно.

### Статусы
- ⚪ `todo` — не начата
- 🟡 `wip` — в работе
- 🟢 `done` — завершена и удовлетворяет Definition of Done
- 🔴 `blocked` — заблокирована (указать чем)

### Definition of Done (общее)
Задача считается `done`, только если:
1. Код соответствует инвариантам системы ([../README.md](../README.md#инварианты-системы-не-нарушать-при-реализации)).
2. Есть проверка работоспособности (запуск/тест/скрин), результат зафиксирован честно.
3. Не нарушены границы: ядро остаётся доменно-независимым, ключи LLM не в клиенте.

### Порядок ведения
- Отмечать статусы прямо в чеклистах плана фазы.
- Открытые вопросы фазы держать в конце её плана; разрешённые — переносить в архитектурные доки.
